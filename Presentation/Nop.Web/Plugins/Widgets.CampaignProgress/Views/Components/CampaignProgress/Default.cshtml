@using Nop.Plugin.Widgets.CampaignProgress.Models.Public
@model List<CampaignWidgetItemModel>

<section class="cp-section">
    <div class="cp-container">
        <div class="cp-header">
            <h2 class="cp-heading">Aktif Projelerimiz</h2>
            <p class="cp-subheading">Her bağış, bir umudun filizlenmesidir</p>
        </div>
        <div class="cp-grid">
            @foreach (var item in Model)
            {
                <a href="/proje/@item.Slug" class="cp-card">
                    @if (!string.IsNullOrEmpty(item.ImageUrl))
                    {
                        <div class="cp-card-img" style="background-image:url('@item.ImageUrl')"></div>
                    }
                    else
                    {
                        <div class="cp-card-img cp-card-img--placeholder"></div>
                    }
                    <div class="cp-card-body">
                        <h3 class="cp-card-title">@item.Title</h3>
                        <div class="cp-progress-wrap">
                            <div class="cp-progress-bar">
                                <div class="cp-progress-fill" style="width:@item.ProgressPercent%"></div>
                            </div>
                            <span class="cp-progress-pct">%@item.ProgressPercent</span>
                        </div>
                        <div class="cp-amounts">
                            <span class="cp-raised">@item.CurrentAmount.ToString("N0") ₺ toplandı</span>
                            <span class="cp-goal">Hedef: @item.GoalAmount.ToString("N0") ₺</span>
                        </div>
                        <span class="cp-btn">Destek Ol <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="vertical-align:middle;margin-left:4px;"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg></span>
                    </div>
                </a>
            }
        </div>
    </div>
</section>

<style>
.cp-section { padding: 60px 20px 60px 0; background: #fff; width: 100%; box-sizing: border-box; }
.cp-container { max-width: 100%; margin: 0; }
.cp-header { text-align: center; margin-bottom: 40px; }
.cp-heading { font-size: 2rem; font-weight: 700; color: #111827; margin: 0 0 10px; }
.cp-subheading { color: #6b7280; font-size: 1.05rem; margin: 0; }
.cp-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 24px; }
.cp-card { display: flex; flex-direction: column; background: #fff; border-radius: 12px; overflow: hidden; box-shadow: 0 2px 12px rgba(0,0,0,.08); text-decoration: none; color: inherit; transition: transform .2s, box-shadow .2s; border: 1px solid #e5e7eb; }
.cp-card:hover { transform: translateY(-4px); box-shadow: 0 8px 24px rgba(37,99,235,.15); }
.cp-card-img { height: 180px; background: center/cover no-repeat; }
.cp-card-img--placeholder { background: linear-gradient(135deg, #2563eb, #3b82f6); }
.cp-card-body { padding: 20px; flex: 1; display: flex; flex-direction: column; gap: 12px; }
.cp-card-title { font-size: 1rem; font-weight: 600; color: #111827; margin: 0; line-height: 1.4; }
.cp-progress-wrap { display: flex; align-items: center; gap: 10px; }
.cp-progress-bar { flex: 1; height: 8px; background: #dbeafe; border-radius: 4px; overflow: hidden; }
.cp-progress-fill { height: 100%; background: linear-gradient(90deg, #2563eb, #3b82f6); border-radius: 4px; transition: width .6s ease; }
.cp-progress-pct { font-size: .8rem; font-weight: 700; color: #2563eb; min-width: 36px; text-align: right; }
.cp-amounts { display: flex; justify-content: space-between; font-size: .82rem; }
.cp-raised { color: #2563eb; font-weight: 600; }
.cp-goal { color: #888; }
.cp-btn { display: inline-block; margin-top: auto; background: #2563eb; color: #fff; text-align: center; padding: 10px 0; border-radius: 8px; font-size: .9rem; font-weight: 600; letter-spacing: .3px; }
.cp-card:hover .cp-btn { background: #1d4ed8; }
@@media (max-width: 600px) { .cp-grid { grid-template-columns: 1fr; } .cp-heading { font-size: 1.5rem; } }
</style>
